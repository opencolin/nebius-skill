# Quickstart

Get up and running with Nebius Skill in 5 minutes.

## Prerequisites

- Claude Code installed ([Download](https://claude.com/claude-code))
- Nebius account ([Create free account](https://nebius.cloud/signup))
- Basic CLI familiarity

## Step 1: Install Nebius CLI

```bash
curl -sSL https://storage.eu-north1.nebius.cloud/cli/install.sh | bash
exec -l $SHELL
nebius version
```

## Step 2: Authenticate

```bash
nebius profile create
# Follow prompts to login via browser
```

Verify authentication:
```bash
nebius iam whoami
```

## Step 3: Deploy Your First Endpoint

Open Claude Code and describe what you want:

```
Deploy a serverless OpenClaw endpoint with GPU
```

Claude will:
1. Create the endpoint with `nebius ai endpoint create`
2. Wait for it to be RUNNING
3. Extract the IP address and metrics
4. Return the dashboard URL

**Expected output:**
```
✓ Endpoint created: endpoint-abc123
✓ Status: RUNNING
✓ Dashboard: http://1.2.3.4:18789
✓ Metrics: http://1.2.3.4:8888/metrics
```

## Step 4: Access Your Endpoint

```bash
# Get endpoint IP
ENDPOINT_IP="1.2.3.4"

# SSH access
ssh nebius@$ENDPOINT_IP

# API access
curl http://$ENDPOINT_IP:8080/v1/models
```

## Common First Tasks

### Deploy a GPU VM for Development

```
Create a GPU VM with 80GB H100 for model development
```

### Setup Kubernetes Cluster

```
Deploy a managed Kubernetes cluster with 3 nodes
```

### Create a Container Registry

```
Setup a container registry for my Docker images
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Services" icon="cube" href="/docs/services">
    Explore all Nebius services
  </Card>
  <Card title="Examples" icon="code" href="/examples/deploy-openclaw">
    Copy-paste deployments
  </Card>
  <Card title="Authentication" icon="lock" href="/docs/authentication">
    Setup advanced auth
  </Card>
  <Card title="Troubleshooting" icon="wrench" href="/docs/troubleshooting">
    Fix issues
  </Card>
</CardGroup>

## Costs

**First-time users get $100 credit** (see [billing](https://nebius.cloud/billing)).

**Example costs:**
- CPU endpoint (1 hour): ~$0.10
- GPU endpoint (1 hour): ~$2.50
- Delete resources to avoid charges

## Need Help?

- **Installation issues**: See [Installation Guide](/docs/installation)
- **Authentication errors**: See [Authentication Guide](/docs/authentication)
- **General problems**: Check [Troubleshooting](/docs/troubleshooting)
- **Chat with community**: [#ai-experiments](https://slack.com)
